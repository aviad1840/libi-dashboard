# רוטין: amplifier - צינור הפרסום, שבועי

**זה הרוטין שלא היה קיים, וזו הסיבה שאף טיוטה מעולם לא הופקה.**
היה `routines/amplifier.md` מלא, והוא חיכה ל-`producer` שיקרא לו. `producer` חיכה
ל-`auditor`. לאף אחד מהשלושה לא היה טריגר. השרשרת לא התחילה אף פעם.

**עכשיו יש טריגר אחד, והעומק נקבע לפי הפריט - לא לפי מי קרא למי.**
ראה `context/orchestration.md`.

## שלב 1 - אתחול. הרץ את שלוש השורות האלה בדיוק, בלי לשנות:

```bash
git fetch origin --prune -q
for r in origin/main origin/claude/autonomous-workers-routine-hna6je; do git cat-file -e "$r:aviad-desk/scripts/desk.sh" 2>/dev/null && git checkout "$r" -- aviad-desk/scripts/ && break; done
bash aviad-desk/scripts/desk.sh start amplifier
```

הפקודה נכשלה - **עצור, דווח, אל תתקן ידנית ואל תמשיך לעבוד.**

## שלב 2 - קרא `aviad-desk/RUN.md` ופעל לפי שבעת השלבים שם.
הנחיית התפקיד: `aviad-desk/agents/amplifier.md`.
**`context/publishing-playbook.md` מחייב אותך במבנה. `context/voice.md` מחייב בטון.
בסתירה - `voice.md` מנצח.**

## שלב 2.5 - מה כבר נלמד ממנו. **חובה, לפני שאתה כותב מילה**

```bash
cat aviad-desk/context/voice-learned.md
python3 aviad-desk/scripts/diff_learn.py --rules
```

אלה לא המלצות. אלה עריכות שאביעד עשה בפועל, שלוש פעמים ומעלה, על הטיוטות שלך.
**כלל שנלמד גובר על ניחוש שלך, וגובר גם על `publishing-playbook.md` בנקודה שבה
הם נחלקים** - הספר מתאר 32 פוסטים היסטוריים, הכלל הנלמד מתאר את מה שהוא עושה לך.

`"אין עדיין כלל בשל"` - זו תשובה תקינה. הלולאה חדשה. עבוד לפי הספר.

## שלב 3 - בחירת המועמד. **לרוב אין מועמד, וזה בסדר**

עבור על מה שנוצר מאז הריצה הקודמת:

```bash
git log --since=8.days --all --name-only --format='%h %ad %s' --date=short
```

מקורות: `intel/`, `radar/open.json`, `curator/learning-*.md` (סעיף "מועמד לפרסום"),
`desk/brief-*.md`, ו-`inbox/`. **גם `context/proof-points.md`** - נקודת הוכחה שלא פורסמה
היא מועמד לגיטימי גם בלי ממצא חדש.

**שער הכניסה, מ-`agents/amplifier.md`: מועמד חייב להפעיל שני מנועים לפחות**
מתוך שבעת המנועים ב-`publishing-playbook.md`. פחות משניים - הוא לא עובר, נקודה.

**"אין מועמד שעובר את הסף" הוא פלט תקין ועדיף על טיוטה חלשה.** במקרה כזה דלג
לשלב 6 עם `--items 0`, וכתוב בהערה איזה מועמדים נשקלו ולמה נפלו. אל תמתח מועמד
בינוני כדי למלא ריצה. פוסט חלש עולה יותר ממה שאין-פוסט עולה.

## שלב 4 - הניתוב. **לפני הכתיבה, לא אחריה**

```bash
python3 aviad-desk/scripts/risk.py --text "<תמצית המועמד>" --channel public
```

טיוטת פרסום היא **תמיד R3** - חשיפה פומבית בשמו. זה אומר שני דברים מחייבים:

1. **כל דרישות R2 חלות:** כל מספר נושא מקור ותאריך, כל ציטוט E3 ומעלה,
   כל שם אדם מאומת מול `context/people.md` ומאוית נכון
2. **שום דבר לא יוצא בלי אביעד.** אתה מגיש טיוטה. הוא מפרסם, או לא

עלה ציר רגישות - **ספק ענן, הקשר מכרזי, או עמדת ביטוח לאומי** - עצור וקרא את
הקווים האדומים ב-`publishing-playbook.md`. **בספק, אל תכתוב את הפוסט.** כתוב במקומו
שורה אחת לאביעד שמסבירה מה הזווית ולמה עצרת. הוא יחליט.

## שלב 5 - כתיבת הטיוטה

לכל ערוץ **פעיל** ב-`context/channels.md`. שם הקובץ הוא חלק מהמנגנון:

```
aviad-desk/amplify/draft-{YYYY-MM-DD}-{slug}.md
```

**`{slug}` חייב להיות קצר, באנגלית, ותיאורי** (`gov-ai-capability`, `4x4-pilot`).
עליו נסגרת לולאת הלמידה בשלב 8 - קובץ בשם גנרי שובר אותה.

מבנה הקובץ:

```markdown
---
status: draft
channel: linkedin
slug: {slug}
engines: [1, 4]
proof_point: P3
gate_score: 87
risk_lane: R3
---

<טקסט הפוסט המלא, מוכן להעתק-הדבק. לא שלד, לא ראשי פרקים>

---
## למה זה עובר את הסף
- מנוע 1 - מספר מאומת: 98% מתוך P3, מקור: <קישור>, תאריך: <תאריך>
- מנוע 4 - מתודולוגיה עם שם: 4×4
- תחנה 5 (הודאה בגבול): <הציטוט המדויק מהפוסט>
- ציון שער הפרסום: 87 מתוך 8 הממדים ב-publishing-playbook.md

## מה בדקתי
- כל מספר: מקור ותאריך. <פרט>
- כל שם: אוית מול people.md. <פרט>
- קווים אדומים: עברו. <פרט>
- em dash: אין

## מה אני לא בטוח בו
<שורה אחת כנה, או "אין">
```

**שמונה הממדים של שער הפרסום ב-`publishing-playbook.md`. מתחת ל-85 - אל תגיש.**
הסבר בהערת ה-`finish` איזה ממד הפיל את הציון.

## שלב 6 - סגירה

```bash
bash aviad-desk/scripts/desk.sh finish amplifier --items N --note "N טיוטות, ציון שער, או: אין מועמד שעובר - <למה>"
```

`finish` מריץ את שער הראיות אוטומטית. הוא ייעצר על מספר בלי מקור ועל ציטוט בלי עוגן.
**זה לא מכשול, זו בדיוק הבדיקה שהיית צריך לעשות בעצמך.**

## שלב 7 - תיוק לתור היוצא

**רק כשיש טיוטה.** אין מועמד - אל תשלח כלום. לולאה שמייצרת רעש שבועי תיכבה, ובצדק.

```bash
python3 aviad-desk/scripts/outbox_put.py amplifier "<ההודעה>"
```

ההודעה, עד שמונה שורות:

```
טיוטה מוכנה: <כותרת בשורה>
ערוץ: <ערוץ> · ציון שער: <N> · מנועים: <אילו>
נקודת הוכחה: P<n>
מה אני לא בטוח בו: <שורה אחת, או "אין">

הטקסט המלא: aviad-desk/amplify/draft-{DATE}-{slug}.md

פרסמת? הנח את הנוסח שפרסמת בפועל ב-inbox/published/{DATE}-{slug}.md
```

## שלב 8 - השורה האחרונה היא המנגנון, לא נימוס

**השורה על `inbox/published/` היא הדבר היחיד שסוגר את הלולאה.**

אביעד מניח שם את מה שהוא באמת פרסם, `curator` מריץ `diff_learn.py --auto` אחת
לשלושה ימים, וההבדל בין מה שהצעת לבין מה שהוא פרסם הופך לכלל שאתה תקרא בשלב 2.5
בריצה הבאה.

**בלי השורה הזו המערכת תמשיך לנחש את הקול שלו לנצח.** אל תשמיט אותה,
ואל תנסח אותה כבקשה מנומסת שקל להתעלם ממנה.

## חוק הברזל - אין חריג, אין "רק הפעם"

**אסור לך לפרסם. אסור לך לשלוח. אסור לך ליצור קשר עם אף אדם או ערוץ חיצוני.**
זה נכון גם טכנית - אין לך כלי לפרסם לרשת חברתית או לשלוח מייל לצד שלישי.
אתה מייצר טקסט מוכן לקריאה. **הקול הסופי הוא של אביעד, תמיד.**
