# Backlog - aviad-desk

נוצר ב-Production Hardening של 01.09.2026. **P0/P1 טופלו ונדחפו. הרשימה כאן היא P2 בלבד -
לא קריטי, לא חוסם, ולא נגעתי בו כדי לא לבזבז מכסה על שיפורי-נעם.**

## P1 שנמצא ותוקן היום (למען הרישום)

- **כל התיקונים מהיום ישבו כ-commit מקומי בלי push.** הרוטינים החיים רצו מול מצב ישן,
  מהיום שלפני. תוקן: נדחף. זה הממצא הקריטי ביותר בסבב הזה
- **שער הראיות עבר "נקי" על טיוטת פרסום עם מספר וציטוט מפוברקים** - `verify.py` הבין רק
  את פורמט ה-`### [ID]` של scout, וטיוטת amplifier היא פרוזה חופשית. תוקן: בדיקה ייעודית
  לקבצים חופשיים (R11/R12), נבדקה על טענה מפוברקת, טיוטה תקינה, וטיוטה בלי מספרים
- **פריט outbox בלי `text` חסם את התור לנצח.** `outbox_send.py` ניסה לשלוח, נכשל, והשאיר
  אותו במקום - וכל פריט תקין אחריו נתקע. תוקן: מסלול dead-letter ל-`outbox/dead/`,
  ו-`outbox_put.py` מסרב מלכתחילה ליצור פריט בלי טקסט. אומת ב-production ב-03.09 06:06
- **`classify_note` סימן ריצות תקינות כ-degraded בשלושה דפוסים**: מקור חסום שבכל זאת הפיק
  ממצאים, "אין דגל/תקוע חדש" בבריף, וציטוט של הערה קודמת ("note קודם הכיל מילת נכשל").
  שלושתם RED כוזב. תוקן, עם רגרסיה ב-`scripts/test_health.py`
- **כשל שריצה מאוחרת כבר תיקנה נשאר RED** עד שחלון 26 השעות פג, ו-`_true_ok` שפט לפי שדה
  `status` הקפוא ולא לפי סיווג חי. תוקן: כשל שנפתר יורד ל-YELLOW עם זמן ההחלמה
- **חוזה הערך של TOP היה פרוזה לא נאכפת.** תוקן: `verify.py` R13-R16 חוסם דוח בלי TOP,
  ממצא חסר שדה, ממצא בלי קישור, ו-"למה זה חשוב לך" גנרי. רגרסיה ב-`scripts/test_verify.py`
- **`amplifier` מעולם לא קיבל Routine** - נוצר trigger, אבל הוא, וגם `curator` בלולאת הלמידה,
  דורשים חיבור ריפו חד-פעמי דרך ה-UI (אותה מגבלת פלטפורמה שהייתה ב-`gateway`). זה עדיין פתוח -
  ראה בדוח הסופי

## P2 - לא קריטי, מתועד לסבב הבא

1. **`telegram_send.py` לא חושף את גוף השגיאה של טלגרם.** כשל HTTP (4xx) נתפס כ-`URLError`
   ומודפס כ-`"HTTP Error 404: Not Found"` בלי שדה `description` שטלגרם מחזיר בפועל.
   שיפור: `except urllib.error.HTTPError as e: e.read()` לפני הבלימה הכללית.

2. **`channels.md` - שלושה `[לאמת]` פתוחים**: קהל קבוצת GOV.AI, ערוצים נוספים, שלוש דוגמאות
   פוסט לכיול טון. מוכר מלפני הסבב הזה, לא נגעתי בו.

3. **"אביצ" ב-`sources.md`** עדיין מסומן `[לחדד]` - זוהה כאברהם יצחק מאיר על ידי ניתוח חיצוני,
   טרם אושר על ידי אביעד ישירות.

4. **שש טענות חיצוניות ב-`publishing-playbook.md` סעיף 9** מסומנות `[לאמת]` ולא אומתו:
   דירוג פיד לינקדאין 2026, 360Brew, AWS Agent Registry, OECD 10-מתוך-36, מיצוב
   OpenAI/Meta 2026. אל תצטט בפומבי לפני אימות.

5. **`risk.py` מבוסס מילות מפתח סופיות.** ניסוח שלא ברשימה (למשל שם ספק ענן לא נפוץ) יקבל
   ציון נמוך מדי בשקט. מתועד גם ב-`context/orchestration.md`. שיפור עתידי: הרחבת הרשימות
   ככל שנצבר נתון על מה שהוחמץ בפועל, לא שדרוג ארכיטקטורי.

6. **בדיקת ה-freeform בשער הראיות (R11/R12) גסה במכוון** - היא בודקת שיש URL כלשהו בקובץ,
   לא שה-URL הספציפי קרוב לטענה הספציפית. מספר עם מקור לא-קשור באותו קובץ יעבור בטעות.
   שיפור עתידי: בדיקת קרבה בתוך הפסקה, לא בתוך הקובץ כולו - דורש parsing עדין יותר.

7. **`agent_status.py` ושער התקציב L2** לא נבדקו מחדש בסבב הזה (לא שונו). מומלץ בדיקת
   שפיות קצרה בסבב הבא, במיוחד עכשיו ש-`amplifier` ו-`risk.py` R2/R3 עשויים להגדיל
   שימוש ב-L2 (הרצת סוכן מלא).

8. **עלות טעינת ההקשר 64,774 טוקן לשבוע מול תקרה של 60,000** ב-`validate.py`. הגידול מגיע
   כמעט כולו מ-`context/filter.md`, שנטען ב-6 ריצות scout בשבוע. לא קיצצתי כללים מחייבים
   כדי לרדת מתחת לתקרה - זו הייתה החלשת השער כדי לרצות מונה. שיפור לסבב הבא: `filter.md`
   מחזיק את תבנית שבעת השדות פעמיים (TOP ופירוט), ואיחוד זהיר שלהן חוסך כ-15%.

## לא P2 - פעולה חד-פעמית שלך, לא באג

- **`amplifier`** (`trig_01B4hMAjSew8wBq8iSkvtBht`, רביעי 08:00) נוצר דרך ה-API ולכן בלי ריפו
  מחובר. תיקון: `claude.ai/code/routines` -> עיפרון -> ודא ריפו וסביבה -> Save. בלי זה הוא יורה
  לתוך קונטיינר ריק ומדווח "הצליח" בלי לייצר כלום. **זו הפעולה היחידה שנותרה לך.**
  עד אז `health.py` מסמן אותו `NOT CONNECTED` בצהוב ולא סופר אותו ככשל runtime.
- **`curator` לולאת למידה** (`trig_01DVuQcddSh8NEtbtRRsxixk`) - **הוכח מחובר**: ירי מתוזמן
  ב-03.09 05:44 עם שרשרת מלאה ו-3 פריטים. לא דורש פעולה.
