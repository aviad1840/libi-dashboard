# Runner: Grok Bot (דרך Cursor Pro)

## מיפוי משתנים
| משתנה | ערך |
|---|---|
| `{CONTEXT}` | `/workspace/aviad-desk/context/` |
| `{OUT}` | `/workspace/aviad-desk/<namespace>/` |
| `{DATE}` | `YYYY-MM-DD` |
| `{WEEK}` | `YYYY-Wnn` |

## הרכבת הנחיית המערכת
```
[תוכן agents/<id>.md]

--- התאמת סביבה ---
קרא בתחילת כל ריצה: {CONTEXT}<כל קובץ ברשימת context>.md
כתוב אך ורק אל {OUT}. כל נתיב אחר אסור.
בסיום: git add {OUT} && git commit && git push
```

## תזמון (Routines)
| סוכן | תזמון |
|---|---|
| scout - יומי | 05:30 א-ה |
| chief-of-staff | 06:15 א-ה |
| scout - שבועי | 06:00 ראשון |
| radar | 07:00 שני + 07:00 שישי |
| advocate | 07:00 שלישי |
| rival | 07:00 רביעי |
| relations | 07:00 חמישי |
| curator | 08:30 חמישי |

**16 ריצות שבועיות. ריצת עומק אחת בלבד (advocate).**

## Auto-Review
**דורש אישור:** כל תקשורת יוצאת, טופס, הרשמה, פרסום, תגובה, הורדת קבצי הפעלה, כתיבה מחוץ לריפו, כל דבר עם אמצעי תשלום.
**מותר:** גלישה לרשימה הלבנה, קריאה, כתיבה ל-namespace, git.

## אילוצים ידועים של הפלטפורמה
1. **כל הבוטים חולקים מחשב ענן אחד** - קבצים, עוגיות דפדפן והרשאות מסוף משותפים. בוט נפרד אינו גבול אבטחה.
2. **אין תמיכה ב-Legacy Privacy Mode.** אימון ושמירה נקבעים בהגדרות חשבון Cursor.
3. **thread לא חסום** - מתנפח ומסתכם אוטומטית. **הזיכרון האמיתי הוא הריפו.**
4. **אין תקרת הוצאות.** ראה `runners/COST.md`.

## נוהל איפוס רבעוני
צור בוט חדש → הזרק `MEMORY/<id>.md` + `agents/<id>.md` → אמת ריצה אחת → מחק את הישן.
**זה תכנון, לא תקלה.**

## יום 1 - push ידני
בשלוש הריצות הראשונות: הבוט כותב ל-`{OUT}` ועוצר. אתה בודק ודוחף ידנית.
אחרי שלוש ריצות תקינות - הפעל push אוטומטי.
