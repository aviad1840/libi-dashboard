# runners - שכבת ההתאמה

**זו השכבה היחידה שתלויה בספק.** להחליף מנוע = לכתוב קובץ חדש כאן. `agents/` ו-`context/` לא זזים.

כל רנר מגדיר: איך נטען ההקשר, מה `{OUT}` בפועל, איך מתוזמנת ריצה, איך התוצר מגיע לריפו, ואיזה גבולות אכיפה קיימים בפלטפורמה.
